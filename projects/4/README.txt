Leonardo Azopardo

To compile type:

'make'

To run type:

'java -classpath .:stdlib.jar:algs4.jar' + programToRun + input files

Errors:

1)Task 2: Clustering coefficient is sligtly different (error < 0.1).
2)Task 3: Vertices are correct but different ones, because some of them have the same betweenness or centrality. If you select, to print the right amount, they all show up.

